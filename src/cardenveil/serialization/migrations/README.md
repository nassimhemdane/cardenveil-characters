# Schema migrations

Migration functions will live here when a `schemaVersion` newer than `1` exists. The current
serializer implements the reference `rpsheet` format directly and rejects unknown object fields.
